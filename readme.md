# If you have any asset refrence and you want to make that asset for this game and recolor/restyle it, try this prompt by giving it to gemini

```Recolor this image to match the following palette EXACTLY. Every rule below 
is mandatory, not a suggestion. Do not preserve any of the original image's 
colors except where explicitly stated.

TARGET PALETTE (exact colors, no close approximations):
1. Primary background / interior fill → #322C43 (deep dark purple)
2. Border / frame material → #DE9A60 (warm tan-orange wood, wood-grain 
   texture — NOT stone, NOT grey)
3. Glow / corner accents → #693F55 (maroon-purple) — NOT orange, NOT red
4. Highlight accent (underglow line, small details) → #47F3FF (bright cyan)
5. Text and icons → UNCHANGED. Keep existing gold/white lettering exactly 
   as-is, same font, same wording, same position.

HARD RULES:
- Every pixel in the image must belong to one of the 5 categories above. 
  Nothing in the final result may be a color outside this palette, except 
  unchanged text/icons.
- Do NOT blend, average, or partially mix new colors with the original ones. 
  Fully replace — old color must not be visible anywhere.
- Specifically forbidden: any grey stone texture, any brown/near-black 
  interior, any pure-orange-only glow with no purple mixed in.
- If there are multiple separate elements/buttons in the image, apply this 
  palette identically to all of them — zero variation between elements.
- Do not change layout, proportions, composition, or icon design. Recolor 
  and re-texture only.

Before finalizing, check the result against each of the 5 numbered rules 
above and confirm every one is satisfied.```